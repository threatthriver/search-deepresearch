// This is a placeholder file for the removed database schema
// We're now using browser localStorage for storing chat history

interface File {
  name: string;
  fileId: string;
}

// Mock schema objects
export const messages = {
  id: 'id',
  messageId: 'messageId',
  chatId: 'chatId',
};

export const chats = {
  id: 'id',
};
